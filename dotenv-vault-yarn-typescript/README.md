# examples/dotenv-vault-yarn-typescript

An example of using dotenv-vault with typescript and yarn.

## USAGE

```bash
yarn install
npx ts-node index.ts // compiles and runs in memory
// edit .env file to see changes
```
